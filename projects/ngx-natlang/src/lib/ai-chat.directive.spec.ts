import { AiChatDirective } from './ai-chat.directive';

describe('AiChatDirective', () => {
  it('should create an instance', () => {
    const directive = new AiChatDirective();
    expect(directive).toBeTruthy();
  });
});
